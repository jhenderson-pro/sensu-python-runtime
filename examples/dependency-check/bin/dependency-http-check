#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
ASSET_DIR=$(cd "$SCRIPT_DIR/.." && pwd)
PYTHON_BIN=${PYTHON_BIN:-python}

export PYTHONPATH="$ASSET_DIR/lib${PYTHONPATH:+:$PYTHONPATH}"

exec "$PYTHON_BIN" "$ASSET_DIR/libexec/check_http.py" "$@"
